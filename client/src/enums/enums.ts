export enum VIEW_TYPE_ENUM{
    MAP_VIEW = <any>'MAP_VIEW', LIST_VIEW = <any>'LIST_VIEW'
}

export enum MEM_LOC_ENUN{
    MEMBERSHIP = <any>'MEMBERSHIP', LOCATION = <any>'LOCATION', UNSET = <any>'UNSET'
}

export enum FOUND_USER_DETAILS_TYPE_ENUM{
    MATCH = <any>'MATCH', POKE = <any>'POKE'
}

export enum POKE_TYPE {
    MATCHES = <any>'MATCHES', POKES = <any>'POKES'
}

export enum MESSAGE_MODE{
    AFTER_MATCH_MESSAGE, NORMAL_MESSAGE
}

export enum PAGE_ENUM {
    MESSAGE = <any>'messagePage', FOUND_USER_DETAILS = <any>'foundUserDetailsPage', MATCHES = <any>'matchesPage'
}

export enum NOTIFICATION_TYPE_ENUM{
    MSG, POKE, MATCH
}